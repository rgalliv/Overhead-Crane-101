# Held CFR fetch — 2026-08-28

Fetched from `https://www.osha.gov/laws-regs/regulations/standardnumber/{part}/{section}` (HTTP 200). Public domain.

eCFR (`ecfr.gov`) redirected to a CAPTCHA interstitial (`unblock.federalregister.gov`) and was **not** used as the held copy.

These `.txt` files are paragraph extracts from the OSHA HTML (`span id` + `field-standard-paragraph-body-p`). §1926.1422 has no paragraph spans; the section body was read from `field--name-body`.
